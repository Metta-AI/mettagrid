_target_: mettagrid.map.mapgen.MapGen

width: 100
height: 100
border_width: 1

root:
  _target_: mettagrid.map.scenes.bsp.BSP
  rooms: 10
  min_room_size: 3
  min_room_size_ratio: 0.2
  max_room_size_ratio: 0.2

  children:
    - scene:
        _target_: mettagrid.map.scenes.random.Random
        agents: 1
